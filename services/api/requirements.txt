# Runtime dependencies for the API service.
# Pins MUST match [project.dependencies] in pyproject.toml exactly.
# Used by the Docker build for a deterministic, cache-safe install.
fastapi==0.115.6
starlette==0.41.3
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
redis==5.2.1
structlog==24.4.0
httpx==0.28.1
argon2-cffi==23.1.0
pyjwt==2.10.1
email-validator==2.2.0
